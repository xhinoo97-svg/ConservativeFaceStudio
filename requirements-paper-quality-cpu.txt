# Optional Phase03 validation runtime. This file is not part of the production
# installer until offline packaging, license and physical-EliteBook gates pass.
--extra-index-url https://download.pytorch.org/whl/cpu
# PyTorch 2.1.2 was built against the NumPy 1.x ABI. Keep the exact compatibility
# family already exercised by the frozen Phase02 Windows qualification.
numpy>=1.26,<2; platform_system == "Windows"
torch==2.1.2+cpu; platform_system == "Windows"
torchvision==0.16.2+cpu; platform_system == "Windows"
